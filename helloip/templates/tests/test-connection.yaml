apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hello_ip.fullname" . }}-test-connection"
  labels:
    {{- include "hello_ip.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hello_ip.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
