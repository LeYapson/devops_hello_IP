apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello_ip.fullname" . }}
  labels:
    {{- include "hello_ip.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hello_ip.selectorLabels" . | nindent 4 }}
